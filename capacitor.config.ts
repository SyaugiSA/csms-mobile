import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.syaugisa.csms",
  appName: "csms",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
